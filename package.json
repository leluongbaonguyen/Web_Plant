{
  "name": "lich-sinh-hoat-react-node-tailwind",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run build && npm run start --workspace server",
    "check": "node --check server/src/index.js && node --check server/src/store.js && node --check server/src/validation.js && node --check server/src/exportWord.js"
  },
  "devDependencies": {
    "concurrently": "10.0.4"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}